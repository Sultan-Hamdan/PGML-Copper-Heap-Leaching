# Python 3.14.3, Windows, RTX 5060 Laptop (8.55 GB)
#
#   pip install -r requirements.txt
#
# torch is a nightly build and is not on PyPI. Install it first:
#
#   pip install torch==2.12.0.dev20260408+cu128 \
#       --index-url https://download.pytorch.org/whl/nightly/cu128
#
# cupy is optional. Without it the solver runs on the CPU, correctly but
# slowly. Everything else works unchanged.

numpy==2.4.3
h5py==3.16.0
scipy==1.17.1

# torch==2.12.0.dev20260408+cu128   see note above
# cupy-cuda13x==14.1.1              optional, GPU solver
